

const collision = (bulletLocations, zombieLocations) => {

  for(var i=0; i<bulletLocations.length; i++) {
    for(var j=0; j<zombieLocations.length; j++) {
      console.log(bulletLocations[i], 'this is bullet loc')
    }
  }
}

export default collision;